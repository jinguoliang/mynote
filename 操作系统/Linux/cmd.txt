Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.4
Creation-Date: 2013-10-16T21:02:14+08:00

====== cmd ======
Created Wednesday 16 October 2013

apt-get install 安装  download 只下载     source 下载源码
ldd exefile  查看exefile所链接的运行库
strace exefile 跟踪所所调用的系统调用和信号
free 查看内存容量
tree -d -L 2   -d表示只显示目录 -L 表示显示目录的深度level
wc -m字符数 -l行数 -w单词数

diff f1 f2     比较两个文件的不同 输出文件中<<表示从f1删除行 >>表示向f1添加行   最后就编程了f2 于是就有了patch
patch orignalfile  patchfile

ed 命令行编辑器		[lineno[,lineno]]command [args]

mplayer 0 9 音量  <>切歌

加密
md5sum 计算md5

==== Internet: ====
curl  url > file
wget url
wget -r -p -k -np http://hi.baidu.com/phps   下载整个网站
Nethogs 是一个终端下的网络流量监控工具，它的特别之处在于可以显示每个进程的带宽占用情况，这样可以更直观获取网络使用情况。它支持 IPv4 和 IPv6 协议、支持本地网卡及 PPP 链接。
__webclient:__
lynx
w3m
[[+nmap|nmap]] 
画图
gnuplot  命令行画图

选择默认
sudo update-alternatis --config java
sudo dpkg-reconfigure lightdm

dsl :
pppoeconf
pon dsl-provider
plog
poff

od: 		dump files in octal and other formats
od -c    char

==== little tools: ====
realpath	relative path → absolute path

==== xwindow: ====
Xorg  -configure   		generate a new config file
gnome-screenshot -c -a		-c send the grab to clipboard   -a     area of screen            ------------>   shift+ctrl+0

==== Custom key shortcut: ====
gconf-editor   

==== system: ====
last	 show listing of last logged in users

=== Packages管理: ===
sudo dpkg-reconfigure $(dpkg -l | awk '{print $2}' | grep "^xserver" | tr '\n' ' ')
sudo apt-get  install --reinstall
apt-cache showsrc mypackagename,查看“Directory:”行获知每个软件包的存放位置
apt-get source mypackagename
__dselect a package manages frontend.__

apt-file update
apt-file show packagename

setup gnome advanced settting:
sudo add-apt-repository ppa:ricotz/testing
sudo apt-get update
sudo apt-get install gnome-tweak-tool
gnome-shell-extensions-common

Dsl confiure:
sudo pppoeconf
pon dsl-provider
poff
plog

== File: ==
lsof   查看所有打开的文件
fuser  查看使用某个文件或socket的进程

=== Find ===
:
	 function cgrep()
	 {
		 find . -name .repo -prune -o -name .git -prune -o -type f \( -name '*.c' -o -name '*.cc' -o -name '*.cpp' -o -name '*.h' \) -print0 | xargs -0 grep --color -n "$@"                                 
	}
	
	__grep__:
		grep -o -e <正则>  file   -o 表示只显示匹配的部分
		echo helloworld |grep -o -e 'l\S*r'   输出llowor


=== Disk: ===
1. 查看分区的uuid
	sudo blkid   or  ll /dev/disk/by-uuibad


=== 排序 ===
sort -n -k2 file 根据第二个关键字来排序, -n 表示按数字排序

== High order: ==
chroot: Run a command with a different root directory
syslinux    在u盘上做系统
exec cmd     不单单执行cmd，会用cmd的内核代替当前bash的内核，不会创建新进程

电脑传感器：
sudo apt-get install lm-sensors
sudo sh -c "yes|sensors-detect"
sensors
开关触摸板
synclient touchpadoff=1

